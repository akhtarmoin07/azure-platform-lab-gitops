apiVersion: v1
kind: LimitRange
metadata:
  name: workload-defaults
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 75m
        memory: 96Mi
      default:
        cpu: 300m
        memory: 384Mi
